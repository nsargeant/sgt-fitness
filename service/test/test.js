/*global describe, it */
'use strict';
var assert = require('assert');
var sgtFitnessService = require('../');

describe('sgt-fitness-service node module', function () {
  it('must have at least one test', function () {
    sgtFitnessService();
    assert(false, 'I was too lazy to write any tests. Shame on me.');
  });
});
